{
  "name": "fastify-clerk-drizzle-server",
  "version": "1.0.0",
  "type": "module",
  "description": "A Fastify server with Clerk Auth and Drizzle ORM, optimized for Bun",
  "main": "dist/server.js",
  "scripts": {
    "dev": "bun run format && bun --watch src/server.ts",
    "start": "bun dist/server.js",
    "build": "tsc -p tsconfig.json && tsc-alias -p tsconfig.json",
    "build:prod": "bun run build && tsup && bun scripts/postbuild.mjs",
    "db:generate": "bunx drizzle-kit generate:pg",
    "db:push": "bunx drizzle-kit push:pg",
    "db:studio": "bunx drizzle-kit studio",
    "format": "bunx prettier --write 'src/**/*.{ts,js,json}'",
    "organize-imports": "bunx organize-imports-cli 'src/**/*.ts'"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/formbody": "^8.0.2",
    "@fastify/helmet": "^13.0.1",
    "@fastify/multipart": "^9.2.1",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/sensible": "^6.0.3",
    "@fastify/static": "^8.2.0",
    "@fastify/swagger": "^9.5.1",
    "@fastify/swagger-ui": "^5.2.3",
    "@fastify/type-provider-json-schema-to-ts": "^5.0.0",
    "ajv-errors": "^3.0.0",
    "drizzle-orm": "^0.44.6",
    "fastify": "^5.6.1",
    "postgres": "^3.4.7",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "dotenv": "^17.2.2",
    "drizzle-kit": "^0.31.4",
    "prettier": "^3.6.2",
    "tsc-alias": "^1.8.16",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  },
  "author": "W.K.D.Kavishka",
  "license": "MIT"
}
